import{c as r,o as i,aj as a,j as t,k as o,g as s}from"./chunks/framework.guNMSiFm.js";const n=["src"],c=["src"],p=JSON.parse('{"title":"Consensus","description":"","frontmatter":{},"headers":[],"relativePath":"blockchain/consensus.md","filePath":"blockchain/consensus.md","lastUpdated":1782673701000}'),d={name:"blockchain/consensus.md"},m=Object.assign(d,{setup(l){return(h,e)=>(i(),r("div",null,[e[0]||(e[0]=a('<h1 id="consensus" tabindex="-1">Consensus <a class="header-anchor" href="#consensus" aria-label="Permalink to &quot;Consensus&quot;">​</a></h1><p>Each time you send a transaction to Iroha, it gets put into a queue. When it&#39;s time to produce a new block, the queue is emptied, and the consensus process begins. This process is equal parts common sense and black magic<sup class="footnote-ref"><a href="#fn1" id="fnref1">[1]</a></sup>.</p><p>The mundane aspect is that a special set of peers needs to take the transaction queue and reproduce the same world state. If the world state cannot be reproduced for some reason or another, none of the transactions get committed to a block.</p><p>The consensus starts over from scratch by choosing a different special set of peers. This is where the black magic comes in. There is a number of things that are fine-tuned: the number of peers in the voting process, the way in which subsequent voting peers are chosen, and the way in which the peers communicate that consensus has failed. Because this changes the view of the world, the process is called a <em>view change</em>. The exact reason for why the view was changed is encoded in the <em>view change proof</em>, but decoding that information is an advanced topic that we won&#39;t cover here.</p><p>The reasoning behind this algorithm is simple: if someone had some evil peers and connected them to the existing network, if they tried to fake data, some good™ peers would not get the same (evil™) world state. If that&#39;s the case, the evil™ peers would not be allowed to participate in consensus, and you would eventually produce a block using only good™ peers.</p><p>As a natural consequence, if any changes to the world state are made without the use of ISI, the good™ peers cannot know of them. They won&#39;t be able to reproduce the hash of the world state, and thus consensus will fail. The same thing happens if the peers have different instructions.</p><h2 id="sumeragi" tabindex="-1">Sumeragi <a class="header-anchor" href="#sumeragi" aria-label="Permalink to &quot;Sumeragi&quot;">​</a></h2><p>Sumeragi is Iroha&#39;s Byzantine-fault-tolerant consensus engine. It takes transactions from the queue, has validator peers agree on the same ordered block, and finalizes that block only after enough validators have reproduced the same result and signed the commit certificate.</p>',8)),t("img",{src:o(s)("/sumeragi-round-dataflow.svg"),alt:"Sumeragi proposal-to-commit data flow"},null,8,n),e[1]||(e[1]=a('<h3 id="proposal-and-commit-path" tabindex="-1">Proposal and commit path <a class="header-anchor" href="#proposal-and-commit-path" aria-label="Permalink to &quot;Proposal and commit path&quot;">​</a></h3><p>Sumeragi runs the ledger forward one block height at a time. At each height, one validator acts as proposer for the current view. The proposer drains eligible transactions from the queue, builds a candidate block, and announces the proposal to the active validator set.</p><p>The same Sumeragi pipeline is used in both permissioned and Nominated Proof-of-Stake (NPoS) deployments:</p><ol><li>A validator proposes a block from queued transactions.</li><li>Validators validate the proposal by executing the transactions against the same world state.</li><li>Validators exchange votes and quorum certificates for the current height and view.</li><li>Once the commit quorum is reached, peers commit the block and update their world state.</li></ol><p>Validators sign only data they can reproduce locally. Before voting, a validator checks that the proposal belongs to the expected chain, height, and view; that transaction signatures and limits are valid; that lane routing and executor validation are deterministic; and that executing the payload produces the expected state transition. If the local result differs, the validator rejects the proposal instead of voting for it.</p><p>Votes are small signed consensus messages. They refer to the proposed block, the height, the view, and the validator identity. Collectors aggregate those votes into a quorum certificate or commit certificate. The certificate is the durable proof that enough validators observed the same result for the same block.</p><h3 id="quorum-collectors-and-observers" tabindex="-1">Quorum, collectors, and observers <a class="header-anchor" href="#quorum-collectors-and-observers" aria-label="Permalink to &quot;Quorum, collectors, and observers&quot;">​</a></h3><p>The voting validator count <code>n</code> defines the Byzantine fault budget. For networks with at least four validators, the budget is <code>f = floor((n - 1) / 3)</code> and the commit quorum is <code>2f + 1</code>. For one to three validators, all validators are required for commit, which is useful for development but has no practical offline slack.</p><p>Collectors are a fanout optimization. Instead of every validator sending every vote to every other validator, Sumeragi can select one or more collectors for a height. The collectors assemble votes, publish quorum progress, and reduce the amount of duplicate vote traffic. The effective collector settings are exposed through <code>ops sumeragi collectors</code> and <code>/v1/sumeragi/collectors</code>.</p><p>Observer peers can synchronize committed blocks, but they do not propose, vote, collect votes, or count toward the commit quorum. Use observers when a deployment needs local query capacity, indexing, monitoring, or regional block replication without increasing the number of voting validators.</p><h3 id="view-changes-and-recovery" tabindex="-1">View changes and recovery <a class="header-anchor" href="#view-changes-and-recovery" aria-label="Permalink to &quot;View changes and recovery&quot;">​</a></h3><p>A view is Sumeragi&#39;s attempt to finalize one height with a particular proposer and timing plan. If proposal, payload, vote, or commit progress stalls, the pacemaker can move the height to a later view. A view change does not rewrite a committed block. It changes how validators try to finish the uncommitted height, carrying forward the highest known quorum or commit evidence so peers do not finalize conflicting blocks.</p><p>Payload recovery is separate from the finality decision. A peer might receive a quorum or commit certificate before it has the full block payload. In that case, the peer uses reliable broadcast (RBC) or block sync to recover the payload, verifies it against the advertised hashes, and only then applies the block to the world state and Kura.</p><h3 id="consensus-modes" tabindex="-1">Consensus modes <a class="header-anchor" href="#consensus-modes" aria-label="Permalink to &quot;Consensus modes&quot;">​</a></h3><p>The selected mode controls how the validator set is formed and operated. It is declared in genesis through <a href="/iroha-2-docs/reference/genesis.html"><code>consensus_mode</code></a> and in peer configuration through <code>sumeragi.consensus_mode</code>. Treat it as network-wide state: validators need the same signed genesis, topology, trusted peer data, and effective Sumeragi parameters.</p>',15)),t("img",{src:o(s)("/sumeragi-mode-dataflow.svg"),alt:"Sumeragi consensus mode data flow"},null,8,c),e[2]||(e[2]=a('<table tabindex="0"><thead><tr><th>Mode</th><th>Best fit</th><th>Validator set</th><th>Operational focus</th></tr></thead><tbody><tr><td>Permissioned</td><td>Private, consortium, and operator-managed networks</td><td>Validators come from the trusted peer topology agreed by the deployment</td><td>Keep all validators on the same signed genesis, trusted peers, peer keys, and Sumeragi parameters</td></tr><tr><td>NPoS</td><td>Public or Nexus-oriented networks where validation follows nomination and stake policy</td><td>Validators are selected by the NPoS profile, usually across epochs, and require BLS keys plus Proofs-of-Possession</td><td>Keep stake snapshots, epoch parameters, validator PoPs, and NPoS phase timeouts aligned across the network</td></tr></tbody></table><div class="tip custom-block"><p class="custom-block-title">Permissioned mode</p><p>Use permissioned mode when the validator roster is an explicit operational choice. This is the usual starting point for self-hosted Iroha networks because membership changes are deliberate governance or administrator actions. The important operational rule is that every validator must run with the same view of genesis, trusted peers, BLS Proofs-of-Possession, and Sumeragi parameters. A single peer with a different topology or signed genesis can prevent the network from committing.</p></div><div class="tip custom-block"><p class="custom-block-title">NPoS mode</p><p>Use NPoS mode when the deployment profile expects validator participation to be driven by nomination and stake state. Public SORA Nexus deployments use NPoS, and their generated profiles include the BLS validator identities, Proofs-of-Possession, epoch settings, and Sumeragi NPoS parameters needed at startup. Epoch changes can replace the active validator set at defined heights, so operators need to monitor both consensus health and the stake or nomination state that feeds the next roster.</p></div><h2 id="multilane-consensus" tabindex="-1">Multilane consensus <a class="header-anchor" href="#multilane-consensus" aria-label="Permalink to &quot;Multilane consensus&quot;">​</a></h2><p>Iroha&#39;s multilane consensus path is implemented through Nexus lane and dataspace configuration. It does not start a separate consensus instance for each lane. Sumeragi still finalizes one ordered block stream; lanes describe how transactions are routed, scheduled, accounted for, and stored inside that stream.</p><p>The runtime configuration builds three pieces of lane state:</p><ul><li><code>lane_catalog</code>: the configured lanes, each with a numeric <code>LaneId</code>, alias, dataspace, visibility, storage profile, proof scheme, and metadata.</li><li><code>dataspace_catalog</code>: the configured dataspaces, each with a numeric <code>DataSpaceId</code> and a fault-tolerance value used for relay committee sizing.</li><li><code>routing_policy</code>: the default lane/dataspace pair and ordered routing rules that can match accounts or instruction paths.</li></ul><p>When a transaction enters the queue, the lane router resolves it to a <code>RoutingDecision { lane_id, dataspace_id }</code>. In single-lane mode this is always lane <code>0</code> and the universal dataspace. In Nexus mode, the configured router applies dataspace-scoped rules, settlement routing, account rules, explicit routing rules, and finally the default route. The resolved lane and dataspace must exist in their catalogs, and the lane must be bound to the resolved dataspace; otherwise the transaction is rejected before it is queued.</p><p>The queue keeps this routing decision with the transaction hash so that later stages do not have to infer it again. Proposal construction then uses the lane metadata in two ways:</p><ul><li>It interleaves transactions by lane so one lane does not dominate the block just because its transactions were queued first.</li><li>It applies per-lane transaction execution unit (TEU) limits. Transactions that would exceed a lane&#39;s configured capacity are deferred and requeued, except that the first overweight transaction for a lane can be admitted to avoid livelock.</li></ul><p>During reliable broadcast, Sumeragi aggregates the proposed payload by lane and dataspace. The recorded totals include transaction count, broadcast chunks, payload bytes, and TEU. After commit, those totals become the lane and dataspace commitment snapshots exposed through Sumeragi status. If a block contains lane settlement receipts, block processing also creates lane settlement commitments and relay envelopes that bind the block header, commit certificate, data-availability commitment hash, settlement proof, and lane payload size.</p><h2 id="reliable-broadcast-rbc" tabindex="-1">Reliable broadcast (RBC) <a class="header-anchor" href="#reliable-broadcast-rbc" aria-label="Permalink to &quot;Reliable broadcast (RBC)&quot;">​</a></h2><p>Reliable broadcast (RBC) is Sumeragi&#39;s payload dissemination and recovery path. It helps validators and observers obtain the block body that belongs to a proposal or commit certificate, especially when a <code>BlockCreated</code> message, block-sync update, or direct payload transfer is delayed or lost.</p><p>RBC works at the payload level. The proposer announces an RBC session for a block height, view, and payload hash, then sends payload chunks across the commit topology. Peers track chunk receipt, validate the recovered payload against the advertised hash, and exchange <code>READY</code> and <code>DELIVER</code> signals once enough validators have observed the same payload. Sessions are bounded by TTL, chunk, fanout, pending-stash, and persisted-store limits so recovery traffic cannot grow without limit.</p><p>RBC is not a separate consensus decision and it does not replace the commit certificate. A block still finalizes only when the peer has a valid commit certificate and the matching payload locally. When data availability is enabled, RBC contributes availability evidence and payload recovery, but commit progress is driven by the commit certificate plus local payload. If the certificate arrives before the payload, the peer can recover the payload through RBC or block sync and then commit.</p><p>Operationally, RBC is useful for diagnosing missing-payload and data-availability bottlenecks:</p><ul><li><code>iroha --output-format text ops sumeragi rbc status</code> shows aggregate RBC session and throughput counters.</li><li><code>iroha --output-format text ops sumeragi rbc sessions</code> lists active sessions, including chunk progress, readiness, delivery state, and lane/dataspace backlog.</li><li><code>GET /v1/sumeragi/rbc</code> and <code>GET /v1/sumeragi/rbc/sessions</code> expose the same data over Torii; see <a href="/iroha-2-docs/reference/torii-endpoints.html">Torii endpoints</a>.</li><li>Prometheus signals such as <code>sumeragi_rbc_store_pressure</code>, <code>sumeragi_rbc_backpressure_deferrals_total</code>, and per-lane or per-dataspace RBC backlog gauges help separate network loss, chunk recovery, and storage pressure; see <a href="/iroha-2-docs/guide/advanced/metrics.html">Performance and metrics</a>.</li></ul><p>Kura uses the derived lane configuration for storage layout. Each lane receives deterministic storage names such as <code>blocks/lane_000_core</code> and <code>merge_ledger/lane_000_core_merge.log</code>; lane lifecycle changes can provision, retire, or relabel those segments without changing the global block order.</p><hr class="footnotes-sep"><section class="footnotes"><ol class="footnotes-list"><li id="fn1" class="footnote-item"><p>For prospective wizards, the <a href="https://github.com/hyperledger-iroha/iroha/blob/i23-features/docs/source/iroha_2_whitepaper.md" target="_blank" rel="noreferrer">Iroha 2 Whitepaper</a> is a good start. <a href="#fnref1" class="footnote-backref">↩︎</a></p></li></ol></section>',20))]))}});export{p as __pageData,m as default};
