import{_ as i,c as s,o as t,aj as a}from"./chunks/framework.guNMSiFm.js";const u=JSON.parse('{"title":"Troubleshooting Configuration Issues","description":"","frontmatter":{},"headers":[],"relativePath":"help/configuration-issues.md","filePath":"help/configuration-issues.md","lastUpdated":1782673701000}'),o={name:"help/configuration-issues.md"};function r(n,e,h,l,d,c){return t(),s("div",null,[...e[0]||(e[0]=[a(`<h1 id="troubleshooting-configuration-issues" tabindex="-1">Troubleshooting Configuration Issues <a class="header-anchor" href="#troubleshooting-configuration-issues" aria-label="Permalink to &quot;Troubleshooting Configuration Issues&quot;">​</a></h1><p>This section offers troubleshooting tips for issues with Iroha 2 and Iroha 3 configuration. Make sure you <a href="./overview.html#check-the-keys">checked the keys</a> first, as it is the most common source of issues in Iroha.</p><p>If the issue you are experiencing is not described here, contact us via <a href="https://t.me/hyperledgeriroha" target="_blank" rel="noreferrer">Telegram</a>.</p><h2 id="outdated-genesis-on-a-docker-compose-setup" tabindex="-1">Outdated genesis on a Docker Compose setup <a class="header-anchor" href="#outdated-genesis-on-a-docker-compose-setup" aria-label="Permalink to &quot;Outdated genesis on a Docker Compose setup&quot;">​</a></h2><p>When you are using the Docker Compose version of Iroha, you might encounter the issue of one of the peer containers failing with the <code>Failed to deserialize raw genesis block</code> error. This usually means the peer, signed genesis transaction, and generated configuration were produced by different Iroha revisions or profiles.</p><p>Check the failure with these steps:</p><ol><li><p>Use <code>docker ps</code> to check the current containers. Depending on the generated profile, you will usually see <code>hyperledger/iroha:dev</code> containers. The default Docker Compose profile contains four peer containers, although your generated <code>docker-compose.yml</code> may differ.</p></li><li><p>Check the logs and look for the <code>Failed to deserialize raw genesis block</code> error. If you started your Iroha in daemon mode with <code>docker compose up -d</code>, use <code>docker compose logs</code> command.</p></li></ol><p>The way to troubleshoot such an issue depends on the use of Iroha. If this is a basic demo and you do not need to preserve peer data, regenerate a matching localnet or Docker Compose bundle with Kagami:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cargo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --bin</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> kagami</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> localnet</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --build-line</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> iroha3</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --peers</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 4</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --out-dir</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ./localnet</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cargo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --bin</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> kagami</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> docker</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --peers</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 4</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --config-dir</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ./localnet</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --image</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> hyperledger/iroha:dev</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --out-file</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ./docker-compose.yml</span></span></code></pre></div><p>Then remove the old container state and restart from the regenerated <code>genesis.signed.nrt</code>, peer <code>config.toml</code> files, and <code>client.toml</code>.</p><p>If you need to restore the Iroha instance data, do the following:</p><ol><li>Connect the second Iroha peer that will copy the data from the first (failed) peer.</li><li>Wait for the new peer to synchronize the data with the first peer.</li><li>Leave the new peer active.</li><li>Update the genesis and configuration files of the first peer only as part of a coordinated migration.</li></ol><div class="info custom-block"><p class="custom-block-title">INFO</p><p>There is no general automatic rewrite path for replacing genesis on a live network. Treat this as a coordinated migration: preserve the old state, bring up compatible peers, and only move validators to the new configuration after the operators agree on the migration plan.</p></div><h2 id="multihash-format-of-private-and-public-keys" tabindex="-1">Multihash Format of Private and Public Keys <a class="header-anchor" href="#multihash-format-of-private-and-public-keys" aria-label="Permalink to &quot;Multihash Format of Private and Public Keys&quot;">​</a></h2><p>If you look at the <a href="/iroha-2-docs/guide/configure/client-configuration.html">client configuration</a>, you will notice that the keys there are given in <a href="https://github.com/multiformats/multihash" target="_blank" rel="noreferrer">multi-hash format</a>.</p><p>If you&#39;ve never worked with multi-hash before, it is natural to assume that the right-hand-side is not a hexadecimal representation of the key bytes (two symbols per byte), but rather the bytes encoded as ASCII (or UTF-8), and call <code>from_hex</code> on the string literal in both the <code>public_key</code> and <code>private_key</code> instantiation.</p><p>It is also natural to assume that calling <code>PrivateKey::try_from_str</code> on the string literal would yield only the correct key. So if you get the number of bits in the key wrong, e.g. 32 bytes vs 64, that it would raise an error message.</p><p><strong>Both of these assumptions are wrong.</strong> Unfortunately, the error messages don&#39;t help in de-bugging this particular kind of failure.</p><p><strong>How to fix</strong>: use <code>hex_literal</code>. This will also turn an ugly string of characters into a nice small table of obviously hexadecimal numbers.</p><div class="warning custom-block"><p class="custom-block-title">WARNING</p><p>Even the <code>try_from_str</code> implementation cannot verify if a given string is a valid <code>PrivateKey</code> and warn you if it isn&#39;t.</p><p>It will catch some obvious errors, e.g. if the string contains an invalid symbol. However, since we aim to support many key formats, it can&#39;t do much else. It cannot tell if the key is the <em>correct</em> private key <em>for the given account</em> either, unless you submit an instruction.</p></div><p>These sorts of subtle mistakes can be avoided, for example, by deserialising directly from string literals, or by generating a fresh key-pair in places where it makes sense.</p>`,21)])])}const k=i(o,[["render",r]]);export{u as __pageData,k as default};
