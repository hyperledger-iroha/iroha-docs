import{_ as t,c as a,o,aj as r}from"./chunks/framework.guNMSiFm.js";const m=JSON.parse('{"title":"World","description":"","frontmatter":{},"headers":[],"relativePath":"blockchain/world.md","filePath":"blockchain/world.md","lastUpdated":1782673701000}'),s={name:"blockchain/world.md"};function i(d,e,n,c,l,h){return o(),a("div",null,[...e[0]||(e[0]=[r('<h1 id="world" tabindex="-1">World <a class="header-anchor" href="#world" aria-label="Permalink to &quot;World&quot;">​</a></h1><p><code>World</code> is the global entity that contains other entities. The <code>World</code> consists of:</p><ul><li>Iroha <a href="/iroha-2-docs/guide/configure/client-configuration.html">configuration parameters</a></li><li>registered peers</li><li>registered domains</li><li>registered <a href="/iroha-2-docs/blockchain/triggers.html">triggers</a></li><li>registered <a href="/iroha-2-docs/blockchain/permissions.html#permission-groups-roles">roles</a></li><li>registered <a href="/iroha-2-docs/blockchain/permissions.html#permission-tokens">permission token definitions</a></li><li>permission tokens for all accounts</li><li><a href="/iroha-2-docs/blockchain/permissions.html#runtime-validators">the chain of runtime validators</a></li></ul><p>When domains, peers, or roles are registered or unregistered, the <code>World</code> is the target of the (un)register <a href="/iroha-2-docs/blockchain/instructions.html">instruction</a>.</p><h2 id="world-state-view-wsv" tabindex="-1">World State View (WSV) <a class="header-anchor" href="#world-state-view-wsv" aria-label="Permalink to &quot;World State View (WSV)&quot;">​</a></h2><p>World State View is the in-memory representation of the current blockchain state. It includes the <code>World</code>, committed block hashes, transaction indexes, and peers elected for the current epoch. Full block payloads are served from Kura rather than duplicated as mutable WSV data.</p><p>The WSV is the state that queries read and that block execution mutates. It is not the durable source of truth by itself. Durable history is stored in <a href="#kura-storage">Kura</a>, and the WSV can be rebuilt from Kura blocks or loaded from a state snapshot and then caught up by replaying newer Kura blocks.</p><h3 id="what-the-wsv-tracks" tabindex="-1">What the WSV Tracks <a class="header-anchor" href="#what-the-wsv-tracks" aria-label="Permalink to &quot;What the WSV Tracks&quot;">​</a></h3><p>The WSV is broader than the <code>World</code> object. In practice it contains:</p><ul><li>the <code>World</code>: parameters, peers, domains, accounts, assets, NFTs, roles, permissions, triggers, executor data, and other registered data-model objects</li><li>committed block hashes and the latest committed height</li><li>transaction-to-block indexes used by queries and receipts</li><li>the current and previous commit topology used by consensus</li><li>in-memory indexes derived from committed blocks, such as data-availability commitments, receipt cursors, pin intents, and query projection markers</li><li>runtime configuration snapshots needed for deterministic block execution, such as cryptography, governance, pipeline, content, settlement, and Nexus settings</li></ul><p>Queries normally receive a read-only <code>StateView</code> over these structures. A view is a consistent snapshot for query execution; it does not allow direct mutation of the WSV.</p><h3 id="how-the-wsv-changes" tabindex="-1">How the WSV Changes <a class="header-anchor" href="#how-the-wsv-changes" aria-label="Permalink to &quot;How the WSV Changes&quot;">​</a></h3><p>WSV changes are staged before they are committed. Block execution creates a block-scoped state overlay, and each accepted transaction applies its instructions in a transaction-scoped overlay. Data triggers invoked by those transactions run in the same block context. Time triggers are evaluated after transaction effects for the block.</p><p>After consensus commits a block, the peer first enqueues the committed block in Kura. If this enqueue step fails, the WSV is not advanced and the consensus loop retries or requeues the block payload. When the block is accepted into Kura&#39;s queue, Iroha applies the post-execution block effects, updates derived indexes, and commits the staged WSV changes under a state-view lock. This keeps readers from observing a partially committed block.</p><p>The consensus-critical rule is that peers must reach the same WSV from the same committed blocks. Direct local edits to WSV data bypass instructions and will make peers disagree during validation or replay.</p><h3 id="startup-and-replay" tabindex="-1">Startup and Replay <a class="header-anchor" href="#startup-and-replay" aria-label="Permalink to &quot;Startup and Replay&quot;">​</a></h3><p>On startup, Iroha initializes Kura first and learns the stored block height. It then tries to load a state snapshot. If no snapshot is available, or if a snapshot is rejected as recoverable, Iroha creates an initial state and replays committed blocks from Kura. If a snapshot is valid but behind Kura, only the missing height range is replayed.</p><p>Replay validates each stored block, reconstructs the commit roster for that height, applies the block effects to the WSV, and commits the resulting state. This means Kura is the recovery path for the WSV, while snapshots are an optimization that avoid replaying the whole chain.</p><h2 id="kura-storage" tabindex="-1">Kura Storage <a class="header-anchor" href="#kura-storage" aria-label="Permalink to &quot;Kura Storage&quot;">​</a></h2><p><em>Kura</em> is Iroha&#39;s persistent block storage. It stores signed blocks and recovery metadata. It does not store a second mutable copy of the WSV.</p><p>Kura storage is rooted at <a href="/iroha-2-docs/reference/peer-config/params.html#param-kura-store-dir"><code>kura.store_dir</code></a>. Within that root, block data is split by lane or segment. The primary files for a segment are:</p><table tabindex="0"><thead><tr><th>Path</th><th>Purpose</th></tr></thead><tbody><tr><td><code>blocks/&lt;segment&gt;/blocks.data</code></td><td>Contiguous Norito-framed signed block payloads.</td></tr><tr><td><code>blocks/&lt;segment&gt;/blocks.index</code></td><td>Fixed-size <code>(start, length)</code> entries that map block height to bytes in <code>blocks.data</code>.</td></tr><tr><td><code>blocks/&lt;segment&gt;/blocks.hashes</code></td><td>Block hashes by height for fast lookup and startup validation.</td></tr><tr><td><code>blocks/&lt;segment&gt;/blocks.count.norito</code></td><td>Durable commit marker recording how many block index entries are safe to use.</td></tr><tr><td><code>blocks/&lt;segment&gt;/da_blocks/</code></td><td>Evicted block payloads kept outside <code>blocks.data</code> when disk-budget enforcement moves old bodies out of the hot file.</td></tr><tr><td><code>blocks/&lt;segment&gt;/pipeline/sidecars.norito</code> and <code>sidecars.index</code></td><td>Pipeline recovery sidecars keyed by block height.</td></tr><tr><td><code>blocks/&lt;segment&gt;/pipeline/roster_sidecars.norito</code> and <code>roster_sidecars.index</code></td><td>Recent commit-roster sidecars used by block sync and replay.</td></tr><tr><td><code>merge_ledger/&lt;segment&gt;.log</code></td><td>Merge-ledger entries aligned with committed blocks.</td></tr><tr><td><code>commit-rosters.norito</code></td><td>Retained commit certificates and validator checkpoints for recent blocks.</td></tr></tbody></table><p>Kura keeps a compact in-memory vector for the chain: each height has the block hash and, optionally, the block body. The genesis block remains cached, and the most recent <a href="/iroha-2-docs/reference/peer-config/params.html#param-kura-blocks-in-memory"><code>kura.blocks_in_memory</code></a> non-genesis blocks keep their bodies in memory. Older block bodies are dropped from memory and reloaded from Kura files when needed.</p><p>During initialization, <code>strict</code> mode validates stored blocks from the block payloads and rewrites the hash file if needed. <code>fast</code> mode starts from stored hash/index metadata and falls back to strict initialization if that metadata is inconsistent. If Kura detects a corrupt tail, it prunes storage to the last validated block.</p><p>Kura writes new blocks through a background writer. The writer appends block payloads, hashes, and index entries, then advances the durable count marker according to the configured fsync policy. When disk-budget enforcement is active, Kura can purge retired segments or evict older block bodies into <code>da_blocks/</code> while keeping hashes and index entries available for validation and lookup.</p>',25)])])}const u=t(s,[["render",i]]);export{m as __pageData,u as default};
