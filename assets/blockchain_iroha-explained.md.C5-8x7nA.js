import{_ as a,c as t,o,aj as i}from"./chunks/framework.guNMSiFm.js";const u=JSON.parse('{"title":"Iroha Explained","description":"","frontmatter":{},"headers":[],"relativePath":"blockchain/iroha-explained.md","filePath":"blockchain/iroha-explained.md","lastUpdated":1782673701000}'),r={name:"blockchain/iroha-explained.md"};function n(s,e,c,d,l,h){return o(),t("div",null,[...e[0]||(e[0]=[i('<h1 id="iroha-explained" tabindex="-1">Iroha Explained <a class="header-anchor" href="#iroha-explained" aria-label="Permalink to &quot;Iroha Explained&quot;">​</a></h1><p>Iroha 3 is the Nexus-oriented track of the Hyperledger Iroha workspace. It shares the same core components as Iroha 2 but adds the Nexus execution model for data spaces and multi-lane routing.</p><h2 id="core-building-blocks" tabindex="-1">Core Building Blocks <a class="header-anchor" href="#core-building-blocks" aria-label="Permalink to &quot;Core Building Blocks&quot;">​</a></h2><ul><li><strong><code>irohad</code></strong> runs peers</li><li><strong>Torii</strong> is the client and operator gateway</li><li><strong>Sumeragi</strong> handles consensus</li><li><strong>Norito</strong> is the <a href="/iroha-2-docs/reference/norito.html">canonical binary format</a></li><li><strong>IVM</strong> runs portable smart contracts and bytecode</li><li><strong>Kagami</strong> prepares keys, genesis, profiles, and localnets</li><li><strong>SORA Nexus service planes</strong> add Soracloud, Inrou, SoraNet, SoraFS, and SoraDNS for app hosting, privacy transport, storage, and naming</li></ul><h2 id="execution-model" tabindex="-1">Execution Model <a class="header-anchor" href="#execution-model" aria-label="Permalink to &quot;Execution Model&quot;">​</a></h2><p>Every change to world state still happens through transactions. Transactions carry instructions or IVM bytecode, and Torii is the main way clients submit them or observe their effects.</p><p>What changes in Iroha 3 is the deployment shape:</p><ul><li>Nexus-aware configurations can define multiple lanes</li><li>data spaces isolate workloads while staying part of the same ledger model</li><li>routing policy decides which lane and dataspace handle a class of work</li></ul><h2 id="multi-dataspace-architecture" tabindex="-1">Multi-Dataspace Architecture <a class="header-anchor" href="#multi-dataspace-architecture" aria-label="Permalink to &quot;Multi-Dataspace Architecture&quot;">​</a></h2><p>A dataspace is a routing and namespace boundary, not a separate blockchain. The runtime still has one <code>World</code>, one transaction model, and one consensus pipeline. Nexus adds catalogs that tell the node how to partition work across lanes and how to name the dataspaces those lanes serve.</p><p>At runtime, a dataspace is represented by a numeric <code>DataSpaceId</code> and catalog metadata. <code>DataSpaceId::UNIVERSAL</code> is reserved as <code>0</code>; the default catalog contains the <code>universal</code> dataspace. Each configured dataspace has:</p><ul><li>a unique numeric ID</li><li>a unique alias such as <code>universal</code>, <code>governance</code>, or <code>zk</code></li><li>an optional description for operator surfaces</li><li>a non-zero <code>fault_tolerance</code> value used to size relay committees</li></ul><p>Lanes are the execution and storage routes bound to those dataspaces. A lane entry carries a <code>LaneId</code>, the <code>DataSpaceId</code> it serves, an alias, visibility (<code>public</code> or <code>restricted</code>), storage profile (<code>full_replica</code>, <code>commitment_only</code>, or <code>split_replica</code>), proof scheme, and optional governance, settlement, and scheduler metadata. The runtime derives per-lane storage geometry from this catalog, including Kura segment names and deterministic key prefixes.</p><p>The routing path is:</p><ol><li>Configuration builds a validated <code>DataSpaceCatalog</code>, <code>LaneCatalog</code>, and <code>LaneRoutingPolicy</code>. Multiple lanes, multiple dataspaces, or non-default routing require <code>nexus.enabled = true</code>.</li><li>The transaction queue asks the active lane router for a <code>RoutingDecision</code> containing a lane ID and dataspace ID.</li><li>Explicit routing rules can match by authority/account or by instruction label. Without a matching rule, the router can derive the dataspace from domain IDs, asset-definition projections, dataspace-scoped permissions, settlement legs, or the authority&#39;s bound account scope.</li><li>The resolved route is checked against both catalogs. Unknown lanes, unknown dataspaces, and lane/dataspace mismatches are deterministic routing errors. If a transaction writes to two different dataspace targets, it is rejected as a conflicting route; cross-dataspace DVP/PVP settlement is routed through the universal coordinator lane.</li><li>Sumeragi and telemetry keep the assignment visible as lane and dataspace activity, backlog, and commitment snapshots.</li></ol><p>This is why object identifiers matter. Domains include the dataspace alias in their ID, for example <code>payments.universal</code>, so domain-scoped writes can be routed. Accounts remain canonical and domainless, so the same account can be bound into different application scopes without changing its <code>AccountId</code>. Asset definitions can carry a domain/dataspace projection, which lets asset operations inherit the correct dataspace route.</p><p>Without Nexus overrides, the node uses a single lane and the <code>universal</code> dataspace. The bundled SORA profile replaces that with a three-lane catalog: <code>core</code> for the universal public lane, <code>governance</code> for governance traffic, and <code>zk</code> for zero-knowledge attachment and contract-deployment traffic.</p><p>Those three defaults exist to separate workload classes:</p><table tabindex="0"><thead><tr><th>Dataspace</th><th>Lane</th><th>Why it exists</th></tr></thead><tbody><tr><td><code>universal</code></td><td><code>core</code></td><td>Reserved default dataspace (<code>DataSpaceId::UNIVERSAL == 0</code>) for ordinary public ledger traffic and fallback routing.</td></tr><tr><td><code>governance</code></td><td><code>governance</code></td><td>Restricted lane for governance and parliament traffic, so control-plane activity is not mixed with general application writes.</td></tr><tr><td><code>zk</code></td><td><code>zk</code></td><td>Restricted lane for zero-knowledge proofs, attachments, and contract deployment routing, keeping proof-heavy workflows separate from normal writes.</td></tr></tbody></table><p>Only <code>universal</code> is the reserved baseline. <code>governance</code> and <code>zk</code> are SORA profile choices encoded in the bundled catalog and routing policy; operators can define a different catalog when they need different dataspace boundaries.</p><h2 id="what-operators-notice-first" tabindex="-1">What Operators Notice First <a class="header-anchor" href="#what-operators-notice-first" aria-label="Permalink to &quot;What Operators Notice First&quot;">​</a></h2><p>Compared with the older single-lane documentation set, operators will notice these changes most quickly:</p><ul><li>richer status and telemetry endpoints</li><li>explicit genesis <code>consensus_mode</code> and PoP-aware topology</li><li>SORA Nexus profiles under <code>defaults/nexus/</code></li><li>more CLI coverage for consensus and operator diagnostics</li></ul><h2 id="read-next" tabindex="-1">Read Next <a class="header-anchor" href="#read-next" aria-label="Permalink to &quot;Read Next&quot;">​</a></h2><ul><li><a href="/iroha-2-docs/blockchain/sora-nexus-services.html">SORA Nexus services</a></li><li><a href="/iroha-2-docs/get-started/launch-iroha-2.html">Launch Iroha 3</a></li><li><a href="/iroha-2-docs/blockchain/world.html">World, WSV, and Kura storage</a></li><li><a href="/iroha-2-docs/reference/genesis.html">Genesis reference</a></li><li><a href="/iroha-2-docs/reference/torii-endpoints.html">Torii endpoints</a></li></ul>',25)])])}const m=a(r,[["render",n]]);export{u as __pageData,m as default};
